name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Test workflow triggered"
          echo "Token: ${{ secrets.GITHUB_TOKEN != '' }}"
          whoami
          which gh
          echo "Success!"